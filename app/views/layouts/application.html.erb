<!DOCTYPE html>
<html>
<head>
  <title>RibbitApp</title>
  <link rel="stylesheet/less" href="/style.less">
  <script type="text/javascript" src="/less.js"></script>
</head>
<body>
	<header>
		<div class="wrapper">
			<img src="/gfx/logo.png">
			<span>Twitter Clone</span>
			<%= link_to "Public Ribbits", ribbits_path %>
			<% if current_user %>
			    <%= link_to "Sair", sessions_destroy_path %>
			<% else %>
			    <%= form_tag sessions_create_path do %>
			        <%= text_field_tag :username, nil, placeholder: "usuário" %>
			        <%= password_field_tag :password, nil,  placeholder: "senha" %>
			        <%= submit_tag "Entrar" %>
			    <% end %>
			<% end %>
		</div>
	</header>
	<div id="content">
		<div class="wrapper">
			<% flash.each do |name, msg| %>
				<%= content_tag :div, msg, class: "flash #{name}" %>
			<% end %>
			<%= yield %>
		</div>		
	</div>
	<footer>
		<div class="wrapper">
			Ribbit - A Twitter Clone Tutorial
			<img src="/gfx/logo-nettuts.png">
		</div>
	</footer>
</body>
</html>
